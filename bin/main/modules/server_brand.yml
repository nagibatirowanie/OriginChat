# Конфигурация модуля ServerBrand
config-version: 1

# Включен ли модуль
enabled: true

# Название ядра сервера, которое будет отображаться в F3 меню
# Поддерживает цветовые коды (&a, &b, &c и т.д.) и HEX-цвета (#RRGGBB)
server_brand: "&bOriginChat"